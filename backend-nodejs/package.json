{
	"name": "northbridge-backend-nodejs",
	"version": "0.1.0",
	"private": true,
	"description": "Node.js backend scaffold for NorthBridge parity with the legacy TypeScript backend.",
	"main": "src/index.js",
	"type": "commonjs",
	"scripts": {
		"dev": "node scripts/dev.js",
		"seed": "node scripts/seed.js",
		"lint": "echo \"lint not configured yet\"",
		"test": "echo \"tests not configured yet\""
	}
}
