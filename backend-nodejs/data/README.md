# Data Folder

This folder is reserved for backend-side reference data and future persistence exports.

For the current scaffold phase:

- `mock-data/seed-data.js` holds the legacy-compatible in-memory dataset.
- No runtime behavior reads from this folder yet.
- Keep any future persisted exports aligned with the frontend model fields documented in `docs/api-contract.md`.