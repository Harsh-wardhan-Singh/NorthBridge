# Scripts Folder

This folder contains lightweight backend utilities for the Node.js scaffold.

Current entries:

- `dev.js`: verifies that the backend scaffold directories and key docs exist.
- `seed.js`: loads the legacy-compatible mock dataset and prints a summary.

These scripts stay backend-only and do not require any frontend changes.